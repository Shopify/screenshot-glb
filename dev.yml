---
name: screenshot-glb

type: node

up:
  - isogun
  - node:
      version: v20.19.0
      yarn: true
      packages:
        - .

commands:
  server:
    aliases: [s]
    run: yarn server

  build: yarn build

  test: yarn lint && yarn test

open:
  "My Application": https://screenshot-glb.myshopify.io
